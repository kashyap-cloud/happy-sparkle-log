name: Build and Push Docker Image

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Registry
        uses: docker/login-action@v3
        with:
          registry: bunker.mantracare.com
          username: ${{ secrets.DOCKER_USERNAME || secrets.REGISTRY_USERNAME || secrets.DOCKER_REGISTRY_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD || secrets.REGISTRY_PASSWORD || secrets.DOCKER_REGISTRY_PASSWORD }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: bunker.mantracare.com/lovable/gratitude_log:1.0
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
